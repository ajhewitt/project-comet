#!/usr/bin/env bash
set -euo pipefail
COMET_ALLOW_DIRTY=1 micromamba run -n comet python -m comet.cli run \
  --ordering both \
  --prereg config/prereg.yaml \
  --paths config/paths.yaml \
  --out artifacts/summary.json
